package com.crudmehra.sampleroomdb.model

import android.os.Parcelable
import androidx.room.Entity
import androidx.room.PrimaryKey
import kotlinx.parcelize.Parcelize

@Parcelize
@Entity(tableName = "student_table") // User Entity represents a table within the database.
data class StudentModel(
    @PrimaryKey(autoGenerate = true)
    val id: Int, // <- 'id' is the primary key which will be autogenerated by the Room library.
    val firstName: String,
    val lastName: String,
    val age: Int
): Parcelable